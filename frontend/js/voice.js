/* placeholder for future voice agent hooks (kept minimal) */
(function(){
  // You can attach Vapi / WebRTC initialisation here later.
  // For now we expose a simple API surface to avoid breaking imports.
  window.ZypherVoice = {
    init(){ /* no-op */ },
    start(){ /* no-op */ },
    stop(){ /* no-op */ }
  };
})();
