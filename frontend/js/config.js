// Central place for environment/config. Tweak when you wire real backends.
window.ZYPHER_CONFIG = {
  brandName: "ZypherAgents",
  // Example endpoints (swap when you add a backend):
  apiBaseUrl: "",        // e.g. "https://api.zypheragents.com"
  chatEndpoint: "",      // e.g. "/chat"
  speechLocale: "en-GB", // Voice recognition language
};
